spring:
  profiles:
    active: local
---
spring:
  profiles: local
  datasource:
    data: classpath:data-h2.sql
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop